<%= form_for(@goods_item) do |f| %>
  <% if @goods_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@goods_item.errors.count, "error") %> prohibited this goods_item from being saved:</h2>

      <ul>
      <% @goods_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :good_id %><br>
    <%= f.number_field :good_id %>
  </div>
  <div class="field">
    <%= f.label :order_id %><br>
    <%= f.number_field :order_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
